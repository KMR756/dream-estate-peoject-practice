import { Button } from "@/components/ui/button";

const App = () => {
  return (
    <div className="flex bg-black min-h-svh flex-col items-center justify-center">
      <Button>Click me</Button>
    </div>
  );
};

export default App;
